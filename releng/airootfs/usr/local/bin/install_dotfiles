#!/usr/bin/env bash

set -euo pipefail

git clone https://github.com/kriogenia/dotfiles.git
cd dotfiles
chmod +x install.sh

# shellcheck disable=SC2046
sudo pacman -S $(cat requirements.txt)

./install.sh
